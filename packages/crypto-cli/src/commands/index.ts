import handleGenerate from './generate';
import handleEncrypt from './encrypt';
import handleDecrypt from './decrypt';
import handleReformat from './reformat';
import handleRevoke from './revoke';
import handleSession from './session';
import handleSign from './sign';
import handleVerify from './verify';

export const Commands = {
  handleGenerate,
  handleEncrypt,
  handleDecrypt,
  handleReformat,
  handleRevoke,
  handleSession,
  handleSign,
  handleVerify,
}
